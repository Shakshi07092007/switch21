#include<stdio.h>
#include<ctype.h>
int main(){
  chars[100];int ch,i,v=0,c=0,d=0;
  gets(s);
  printf("1.vowel2,consonants3.digit\n");
  scanf("%d",&ch);
  for(i=0;s[i];i++){
      char x=tolower(s[i]);
      if(x>='0'&&x<='9')d++;
else if(x=='a'||x=='e'||x=='i'||x=='o'||x=='u')v++;
else if(x>='a'&&x<='z')c++;
}
 switch(ch){
   case1:printf("vowels=%d",v);
break;
  case2:printf("consonants=%",c);
break;
  case3:printf("digit=%d",d);
break;
  default:(printf("invaild");
}
}